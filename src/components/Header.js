import React from "react";
import AppBar from 'material-ui/AppBar';

class Header extends React.Component {
  render() {
    return (
      <AppBar
        title="React Redux Golang Todo App"
      />
    );
  }
}

export default Header;
